<div class="table-scroll"><table class="data-table"><thead><tr><th>Cita</th><th>Paciente</th><th>Atención</th><th>Estado</th><th><span class="sr-only">Acciones</span></th></tr></thead><tbody>
@forelse($citas as $cita)<tr><td><strong>{{ $cita->fecha->format('d/m/Y') }}</strong><small>{{ substr($cita->hora_inicio,0,5) }} — {{ substr($cita->hora_fin,0,5) }} · #{{ $cita->id }}</small></td><td>{{ $cita->paciente->nombre_completo }}</td><td><strong>{{ $cita->especialidad->nombre }}</strong><small>{{ $cita->medico->nombre_completo }}</small></td><td><span class="status-badge {{ $cita->estado }}">{{ \App\Models\Cita::ESTADOS[$cita->estado] }}</span></td><td><a class="table-link" href="{{ route('citas.show',$cita) }}">Detalles ↗</a></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><span>⌕</span><h3>No hay citas para estos filtros</h3><p>Prueba otra fecha o consulta todos los estados.</p></div></td></tr>@endforelse
</tbody></table></div>
{{ $citas->links('partials.pagination') }}
