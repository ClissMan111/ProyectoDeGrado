@if($paginator->hasPages())<nav class="pagination" aria-label="Paginación"><span>Página {{ $paginator->currentPage() }} de {{ $paginator->lastPage() }}</span><div>@if($paginator->onFirstPage())<span>← Anterior</span>@else<a href="{{ $paginator->previousPageUrl() }}" rel="prev">← Anterior</a>@endif @if($paginator->hasMorePages())<a href="{{ $paginator->nextPageUrl() }}" rel="next">Siguiente →</a>@endif</div></nav>@endif
